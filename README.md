# rdptun-android

Experimental Android IPv4 VPN over a real RDP Dynamic Virtual Channel (`rdptun`).

This repository contains an overlay for FreeRDP 3.31.1 plus a GitHub Actions workflow that builds the Android debug APK.
